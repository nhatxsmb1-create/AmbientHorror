enabled: true
debug: false

director:
  nearby-player-radius: 64
  tick-rate: 100
  min-score-threshold: 15

scheduler:
  check-interval: 200

cooldown:
  global-seconds: 45
  per-player-seconds: 90

night-only: true
skip-in-combat: true
combat-timeout-seconds: 10

enabled-worlds:
  - world
